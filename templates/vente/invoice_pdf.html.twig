<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture {{ vente.numeroFacture }}</title>
    <style>
        @page {
            margin: 2cm;
        }
        body {
            font-family: 'DejaVu Sans', sans-serif;
            font-size: 11pt;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }
        .header h1 {
            margin: 0;
            font-size: 24pt;
            color: #2c3e50;
        }
        .info-section {
            margin-bottom: 20px;
        }
        .info-section table {
            width: 100%;
        }
        .info-section td {
            padding: 5px;
            vertical-align: top;
        }
        .info-box {
            border: 1px solid #ddd;
            padding: 10px;
            background-color: #f9f9f9;
        }
        .products-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        .products-table th {
            background-color: #2c3e50;
            color: white;
            padding: 10px;
            text-align: left;
            font-weight: bold;
        }
        .products-table td {
            padding: 8px;
            border-bottom: 1px solid #ddd;
        }
        .products-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .text-right {
            text-align: right;
        }
        .text-center {
            text-align: center;
        }
        .total-row {
            font-weight: bold;
            font-size: 12pt;
            background-color: #ecf0f1 !important;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 9pt;
            color: #7f8c8d;
            border-top: 1px solid #ddd;
            padding-top: 10px;
        }
        .badge {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 3px;
            font-weight: bold;
            font-size: 10pt;
        }
        .badge-success {
            background-color: #27ae60;
            color: white;
        }
        .badge-secondary {
            background-color: #95a5a6;
            color: white;
        }
        .badge-danger {
            background-color: #e74c3c;
            color: white;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>FACTURE</h1>
        <p style="margin: 5px 0;">N° {{ vente.numeroFacture }}</p>
        <p style="margin: 5px 0;">
            <span class="badge {{ vente.statut.value == 'validee' ? 'badge-success' : (vente.statut.value == 'annulee' ? 'badge-danger' : 'badge-secondary') }}">
                {{ vente.statut.label }}
            </span>
        </p>
    </div>

    <div class="info-section">
        <table>
            <tr>
                <td style="width: 50%;">
                    <div class="info-box">
                        <strong>Informations Vente</strong><br>
                        Date: {{ vente.dateVente|date('d/m/Y H:i') }}<br>
                        Vendeur: {{ vente.user ? vente.user.email : 'N/A' }}
                    </div>
                </td>
                <td style="width: 50%;">
                    <div class="info-box">
                        <strong>Client</strong><br>
                        {% if vente.client %}
                            Nom: {{ vente.client.nom }}<br>
                            Tél: {{ vente.client.telephone }}<br>
                            Adresse: {{ vente.client.adresse }}
                        {% else %}
                            Client de passage
                        {% endif %}
                    </div>
                </td>
            </tr>
        </table>
    </div>

    <table class="products-table">
        <thead>
            <tr>
                <th>Produit</th>
                <th class="text-center">Unité</th>
                <th class="text-right">Prix Unit.</th>
                <th class="text-center">Qté</th>
                <th class="text-right">Sous-Total</th>
            </tr>
        </thead>
        <tbody>
            {% for ligne in vente.ligneVentes %}
                <tr>
                    <td>{{ ligne.produit.nom }}</td>
                    <td class="text-center">{{ ligne.unite ? ligne.unite.nom : ligne.produit.uniteDeBase.nom }}</td>
                    <td class="text-right">{{ ligne.prixUnitaire|number_format(2, ',', ' ') }} Ar</td>
                    <td class="text-center">{{ ligne.quantite }}</td>
                    <td class="text-right">{{ ligne.sousTotal|number_format(2, ',', ' ') }} Ar</td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
            <tr class="total-row">
                <td colspan="4" class="text-right">TOTAL</td>
                <td class="text-right">{{ vente.total|number_format(2, ',', ' ') }} Ar</td>
            </tr>
        </tfoot>
    </table>

    <div class="footer">
        <p>Merci pour votre confiance</p>
        <p>Document généré le {{ "now"|date('d/m/Y à H:i') }}</p>
    </div>
</body>
</html>
