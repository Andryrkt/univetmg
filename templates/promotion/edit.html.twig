{% extends 'base.html.twig' %}

{% block title %}Modifier Promotion{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <h1 class="mb-4">Modifier la Promotion : {{ promotion.nom }}</h1>
                
                {{ include('promotion/_form.html.twig', {'button_label': 'Mettre à jour la Promotion'}) }}
            </div>
        </div>
    </div>
{% endblock %}
